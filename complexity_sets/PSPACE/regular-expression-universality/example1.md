<p><strong>Esempio: complemento NFA via costruzione per sottoinsiemi</strong></p><p>Dato NFA N con n stati, il suo DFA complemento ha al più 2ⁿ stati (via costruzione per sottoinsiemi). Testare l'universalità = testare se il linguaggio complemento è vuoto.</p><p>L'algoritmo calcola il DFA on-the-fly usando DFS, tracciando i sottoinsiemi di stati raggiungibili. Spazio: O(n) per frame DFS × O(n) profondità = O(n²) totale — spazio polinomiale, tempo potenzialmente esponenziale.</p>